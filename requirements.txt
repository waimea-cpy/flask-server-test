#----------------------------------------------------------------
# Required packages to install via pip when deploying to Railway
#----------------------------------------------------------------

Flask
flask-bcrypt
